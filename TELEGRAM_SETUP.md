# 📱 НАСТРОЙКА TELEGRAM УВЕДОМЛЕНИЙ

## ✅ Система уже интегрирована

Модуль `src/utils/telegram-notify.js` создан и подключен. Теперь нужно только настроить переменные окружения.

---

## 📋 ШАГ 1: Создать .env файл

Создайте файл `.env` в корне проекта (там же, где `package.json`) со следующим содержимым:

```env
VITE_TELEGRAM_BOT_TOKEN=ваш_токен_бота
VITE_TELEGRAM_CHAT_ID=ваш_chat_id
VITE_TELEGRAM_ENABLED=true
```

**Важно:** В Vite переменные окружения должны начинаться с `VITE_` чтобы быть доступными в клиентском коде.

---

## 🤖 ШАГ 2: Получить Bot Token

1. Откройте Telegram и найдите **@BotFather**
2. Отправьте команду `/newbot`
3. Следуйте инструкциям:
   - Введите имя бота (например: "My Site Notifications")
   - Введите username бота (должен заканчиваться на `bot`, например: `mysite_notify_bot`)
4. BotFather вернет вам токен вида: `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`
5. Скопируйте этот токен и вставьте в `.env` файл

---

## 💬 ШАГ 3: Получить Chat ID

### Вариант 1: Личный чат
1. Напишите боту @userinfobot
2. Он вернет ваш Chat ID (число, например: `123456789`)

### Вариант 2: Канал или группа
1. Добавьте бота в канал/группу как администратора
2. Отправьте любое сообщение в канал/группу
3. Перешлите это сообщение боту @userinfobot
4. Он вернет Chat ID канала/группы (начинается с `-`, например: `-1001234567890`)

### Вариант 3: Через API
1. Напишите вашему боту любое сообщение
2. Откройте в браузере: `https://api.telegram.org/botВАШ_ТОКЕН/getUpdates`
3. Найдите в ответе `"chat":{"id":123456789}` - это ваш Chat ID

---

## 📝 ШАГ 4: Заполнить .env файл

Откройте `.env` файл и замените значения:

```env
VITE_TELEGRAM_BOT_TOKEN=123456789:ABCdefGHIjklMNOpqrsTUVwxyz
VITE_TELEGRAM_CHAT_ID=123456789
VITE_TELEGRAM_ENABLED=true
```

---

## 🔄 ШАГ 5: Перезапустить dev сервер

После изменения `.env` файла нужно перезапустить Vite:

```bash
# Остановите текущий сервер (Ctrl+C)
# Затем запустите снова:
npm run dev
```

---

## ✅ ПРОВЕРКА

1. Откройте сайт в браузере
2. Откройте консоль (F12)
3. Должны увидеть логи:
   ```
   [TelegramNotify] 📱 Инициализация системы Telegram уведомлений...
   [TelegramNotify] ✅ Конфигурация загружена из .env
   [TelegramNotify] 📱 Сбор информации о посетителе...
   [TelegramNotify] ✅ Уведомление успешно отправлено в Telegram
   ```
4. Проверьте Telegram - должно прийти уведомление в формате:
   ```
   📥 Новый визит на страницу
   URL: https://ваш-сайт.com/
   Время: 16.12.2025, 23:33:25
   IP: 44.201.146.1
   Локация: Ashburn, Virginia, United States
   User-Agent: Mozilla/5.0 (Macintosh; Intel Mac OS X 10_15_7)...
   ```

---

## ⚠️ ВАЖНЫЕ ЗАМЕЧАНИЯ

1. **`.env` файл НЕ должен быть в Git!**
   - Убедитесь, что `.env` добавлен в `.gitignore`
   - Не коммитьте токены в репозиторий

2. **Продакшн на Netlify (рекомендуется):**
   - Добавьте переменные окружения без префикса `VITE_`, чтобы их использовала серверная функция:
     - `TELEGRAM_BOT_TOKEN`
     - `TELEGRAM_CHAT_ID`
   - После сохранения переменных сделайте **redeploy** (иначе старый билд не увидит новые значения)

3. **Если нужен доступ к токенам в браузере (локально):**
   - Используйте `.env` с `VITE_TELEGRAM_BOT_TOKEN` и `VITE_TELEGRAM_CHAT_ID`
   - В продакшне предпочтительнее держать токены только на серверной стороне (Netlify Function)

4. **Формат сообщения:**
   - Система автоматически определяет IP и локацию
   - Формат соответствует вашему примеру
   - User-Agent обрезается до 100 символов

---

## 🐛 РЕШЕНИЕ ПРОБЛЕМ

### Уведомления не приходят:
1. Проверьте, что Bot Token правильный
2. Проверьте, что Chat ID правильный
3. Убедитесь, что бот запущен (напишите ему `/start`)
4. Проверьте консоль браузера на ошибки

### Переменные не загружаются:
1. Убедитесь, что переменные начинаются с `VITE_`
2. Перезапустите dev сервер после изменения `.env`
3. Проверьте, что `.env` файл в корне проекта

### Локация не определяется:
- API `ipapi.co` может иметь лимиты
- Если не работает, локация будет "Не определена"
- Это не критично, уведомление все равно отправится

---

**Готово!** После настройки уведомления будут приходить при каждом посещении сайта.
